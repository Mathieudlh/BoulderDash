package model;

/**
 * Created by mathi on 21/06/2017.
 */


import java.awt.Graphics;
import java.awt.image.BufferedImage;

public class Assets {
    
    /*player_down = new BufferedImage[4];
    player_up = new BufferedImage[4];
    player_left = new BufferedImage[3];
    player_left = new BufferedImage[3];

    player_down[0] = sheet.crop(witdh , hight * 4, width, height);
    player_down[1] = sheet.crop(witdh * 1, hight * 4, width, height);
    player_down[2] = sheet.crop(witdh * 2 , hight * 4, width, height);
    player_down[3] = sheet.crop(witdh * 3, hight * 4, width, height);

    player_up[0] = sheet.crop(witdh , hight * 2, width, height);
    player_up[1] = sheet.crop(witdh * 1, hight * 2, width, height);
    player_up[2] = sheet.crop(witdh * 2, hight * 2, width, height);
    player_up[3] = sheet.crop(witdh * 3, hight * 2, width, height);

    player_left[0] = sheet.crop(witdh , hight * 3, width, height);
    player_left[1] = sheet.crop(witdh * 1, hight * 3, width, height);
    player_left[2] = sheet.crop(witdh * 2, hight * 3, width, height);

    player_right[0] = sheet.crop(witdh , hight, width, height);
    player_right[1] = sheet.crop(witdh * 1, hight, width, height);
    player_right[2] = sheet.crop(witdh * 2, hight, width, height);*/

}
